import 'package:flutter/cupertino.dart';

class VisitPageField {
  VisitPageField(
      {@required this.barcode,
      @required this.latitude,
      @required this.longitude,
      @required this.namaOutlet});

  String barcode, latitude, longitude, namaOutlet;
}
